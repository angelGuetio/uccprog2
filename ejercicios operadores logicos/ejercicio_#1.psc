Proceso ejercicio1
	definir a,b,x,yy Como Entero;
	definir s como logico;
	
	a <- 1;
	b <- 0;
	x <- 0;
	yy <- 2;
	s <- (a > b) y (x = yy);
	
	si (a > b) y (x = yy) Entonces
		s = (a > b) y (x = yy);

		
	FinSi
	escribir  "el valor final de la variable  de S es:", s;
	
FinProceso
